请将这份迁移包中的协作与按需多智能体工作流配置到这台电脑的 Codex，作为个人全局默认设置。请实际完成配置和检查，不要仅给出教程。

请先读取迁移包内的 config-to-merge.toml、AGENTS.md 和 agents 目录中的四个 TOML 文件。这些文件是我授权采用的目标配置。

目标配置：
- 主模型 gpt-6-astra，推理强度 medium。
- explorer：gpt-5.6-luna / medium，限定范围的只读探索。
- worker：gpt-5.6-sol / high，具体实现与适当测试。
- researcher：gpt-5.6-luna / medium，聚焦资料与文档查证。
- reviewer：gpt-6-astra / high，按 AGENTS.md 中的风险、并行工作条件或用户明确要求进行独立审核。
- 启用多智能体，最多同时运行三个子代理。
- 有能与主代理工作独立并行的具体子任务时，按需委派；简单或紧密依赖的任务直接完成，不要每次启动全部角色。
- 主代理负责规划、协调、汇总与最终验证，子代理默认不再委派。

分档和升级规则：
- 探索和研究默认 medium；证据不足、来源冲突或复杂跨模块问题，在一次针对性尝试后向主代理报告，由主代理决定澄清、接手或升级，避免重复尝试。
- 较难探索和研究可升级 Luna / high；能力不足时由主代理接手或使用 Sol / high。复杂安全、并发、数据一致性审核才考虑 Astra / xhigh。
- 自定义角色文件会固定模型和推理档位。升级时使用不固定档位的通用/default 代理，并显式传入模型和档位及简明任务上下文；若客户端不支持，明确报告或由主代理接手。不要仅靠提示词声称覆盖了角色文件。
- 子代理只接收必要上下文，返回简洁结论、证据、验证和未解决项；不默认复制整个对话。

执行要求：
1. 查阅当前官方 Codex 文档，并检查本机版本、实际配置目录和可用模型／推理档位。默认配置目录是 ~/.codex；如果设置了 CODEX_HOME，请使用实际目录。
2. 先备份会修改的文件。将 config-to-merge.toml 的设置合并进现有 config.toml；不要整份覆盖，不要重复创建已有的 [agents] 表。主模型字段必须放在 TOML 顶层。
3. 将迁移包 AGENTS.md 中的完整协作原则、分工和升级规则合并进个人全局 AGENTS.md。按语义替换旧版同源规则，避免重复追加旧英文工作流或六段协作原则；保留其他无冲突的用户规则。若旧版同源规则冲突，以本次明确指定的版本为准并说明改动；与其他个人规则存在无法判断的冲突时，仅澄清该冲突。
4. 将四个角色安装到个人配置目录的 agents 子目录。若同名角色已存在，先备份再更新。
5. 保留账号、认证、服务商、插件、MCP、项目路径及权限配置。不要从旧电脑复制密钥、凭据或机器专属路径，也不要降低安全保护。
6. 若当前版本的配置格式发生变化，按官方文档转换成等价设置；若指定模型不可用，明确报告，不要静默替换或声称已生效。
7. 检查所有 TOML 文件语法、角色名称、模型及推理档位、并发上限，以及全局协作、分工和升级规则是否完整且无重复冲突；在可用时验证客户端加载情况。无须为验证而启动付费子代理。
8. 完成后用中文报告实际写入的设置、备份位置，以及是否需要重启客户端或新建任务。区分配置已保存、规则已加载和真实模型调用已验证。

请在现有授权范围内完成以上操作；若系统要求文件写入授权，再发起所需权限申请。
