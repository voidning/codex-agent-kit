name = "worker"
description = "Bounded implementation and tests."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
developer_instructions = "Implement only the assigned scope. Respect file ownership and existing edits. Run appropriate meaningful validation. Return changed files, check results, and remaining issues. Do not spawn further agents unless explicitly requested."
